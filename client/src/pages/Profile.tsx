import { useAuth } from "@/_core/hooks/useAuth";
import { Avatar, AvatarFallback } from "@/components/ui/avatar";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { trpc } from "@/lib/trpc";
import { ShieldCheck, UserRound } from "lucide-react";
import { useEffect, useState } from "react";
import { toast } from "sonner";

export default function Profile() {
  const { user } = useAuth(); const profile = trpc.campusfix.profile.me.useQuery(); const update = trpc.campusfix.profile.update.useMutation({ onSuccess: () => toast.success("Campus profile saved.") });
  const [form, setForm] = useState({ campusId: "", campusRole: "student", department: "", program: "", yearOfStudy: "" });
  useEffect(() => { if (profile.data) setForm({ campusId: profile.data.campusId || "", campusRole: profile.data.campusRole, department: profile.data.department || "", program: profile.data.program || "", yearOfStudy: profile.data.yearOfStudy || "" }); }, [profile.data]);
  return <div className="mx-auto max-w-4xl space-y-6"><section className="motion-enter"><p className="eyebrow">Private workspace context</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-50">Your campus profile</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Profile context helps CampusFix tailor guidance. Keep it minimal and update it whenever your situation changes.</p></section><section className="glass-panel motion-enter rounded-3xl p-6 sm:p-8" style={{ animationDelay: "60ms" }}><div className="flex items-center gap-4 border-b border-white/[0.07] pb-6"><Avatar className="h-14 w-14 border border-cyan-300/20"><AvatarFallback className="bg-cyan-300/[0.10] text-lg text-cyan-100">{user?.name?.slice(0, 1).toUpperCase() || "C"}</AvatarFallback></Avatar><div><h2 className="text-lg font-semibold text-slate-100">{user?.name || "Campus user"}</h2><p className="mt-1 text-sm text-slate-500">{user?.email || "Your identity is managed through your secure sign-in."}</p><div className="mt-3 inline-flex items-center gap-2 rounded-full border border-cyan-300/15 bg-cyan-300/[0.06] px-2.5 py-1 text-[11px] font-medium text-cyan-100"><ShieldCheck className="h-3.5 w-3.5" />{user?.role === "admin" ? "IT administrator access" : "Student access"}</div></div></div><form className="mt-7 grid gap-5 sm:grid-cols-2" onSubmit={event => { event.preventDefault(); update.mutate({ campusId: form.campusId || undefined, campusRole: form.campusRole as "student" | "faculty" | "it_staff", department: form.department || undefined, program: form.program || undefined, yearOfStudy: form.yearOfStudy || undefined }); }}><div className="space-y-2"><Label htmlFor="campus-id">Campus or student ID</Label><Input id="campus-id" value={form.campusId} onChange={event => setForm({ ...form, campusId: event.target.value })} className="border-white/10 bg-white/[0.04]" /></div><div className="space-y-2"><Label>Campus role</Label><Select value={form.campusRole} onValueChange={value => setForm({ ...form, campusRole: value })}><SelectTrigger className="border-white/10 bg-white/[0.04]"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="student">Student</SelectItem><SelectItem value="faculty">Faculty</SelectItem><SelectItem value="it_staff">IT staff</SelectItem></SelectContent></Select></div><div className="space-y-2"><Label htmlFor="department">Department</Label><Input id="department" value={form.department} onChange={event => setForm({ ...form, department: event.target.value })} className="border-white/10 bg-white/[0.04]" /></div><div className="space-y-2"><Label htmlFor="program">Program or area</Label><Input id="program" value={form.program} onChange={event => setForm({ ...form, program: event.target.value })} className="border-white/10 bg-white/[0.04]" /></div><div className="space-y-2"><Label htmlFor="year">Year of study</Label><Input id="year" value={form.yearOfStudy} onChange={event => setForm({ ...form, yearOfStudy: event.target.value })} className="border-white/10 bg-white/[0.04]" /></div><div className="flex items-end"><Button disabled={update.isPending} className="w-full sm:w-auto">{update.isPending ? "Saving…" : "Save profile"}</Button></div></form></section></div>;
}
